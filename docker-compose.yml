version: "3.9"

services:
backend:
  build:
    backend:
      build: ./backend
      container_name: flask_app
      ports:
        - "6000:6000"
      environment: 
        - FLASK_ENV=development
        - FLASK_APP=app.py
      